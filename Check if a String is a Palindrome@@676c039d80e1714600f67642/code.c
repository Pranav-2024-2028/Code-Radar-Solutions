#include<stdio.h>
#include<string.h>
int main(){
    char arr[1000];
    char arr1[1000];
    int temp = arr[i];

    fgets(arr, sizeof(arr),stdin);

    int length = strlen(arr)-1;

    for(int i=length; i>=0; i--){
        printf("%s",arr[i]);
    }

    

   
    
}
